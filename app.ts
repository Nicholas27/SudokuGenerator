let arr: Array<string> = [];

const getDefaultNumbers = (): Array<number> => {
    let numbers: Array<number> = [1,2,3,5,6,7,8,9];
    return numbers;
}

const generateSudoku = () => {
    let numbers: Array<number> = getDefaultNumbers();
    for(var i = 0; i < 9; i++){
        
    }
}